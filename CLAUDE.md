See [AGENTS.md](AGENTS.md) for project conventions, the load-bearing invariants, the release flow, and AI agent instructions.

AGENTS.md is the single source of truth. Claude Code (CLI and Action) reads it directly. This file exists as a pointer for tooling that only looks for `CLAUDE.md` — keep it short; never duplicate content.
